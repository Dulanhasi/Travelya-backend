const express = require('express');
const router = express.Router();

// Placeholder - implement controllers later
router.get('/', (req, res) => {
    res.json({ success: true, message: 'Service provider routes - to be implemented' });
});

module.exports = router;
